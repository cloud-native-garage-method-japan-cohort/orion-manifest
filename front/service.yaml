apiVersion: v1
kind: Service
metadata:
  name: front-app
  labels:
    app: front-app
spec:
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  selector:
    app: front-app
  type: LoadBalancer
